info:
  name: Получение списка для чтения
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}want-to-read"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
