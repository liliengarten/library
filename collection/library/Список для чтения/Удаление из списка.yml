info:
  name: Удаление из списка
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{host}}want-to-read/1"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
