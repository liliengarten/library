info:
  name: История аренды
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}rent-history"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
