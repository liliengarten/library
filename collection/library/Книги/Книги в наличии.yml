info:
  name: Книги в наличии
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}available-books"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
