info:
  name: Блокирование / разблокирование пользователя
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{host}}users/2"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
