info:
  name: Изменить книгу
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{host}}books/1"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
