info:
  name: Выход
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}logout"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
